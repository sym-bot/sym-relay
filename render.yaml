services:
  - type: web
    name: sym-relay
    runtime: node
    plan: free
    buildCommand: npm ci --production
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 8080
      - key: SYM_RELAY_TOKEN
        sync: false
      - key: LOG_LEVEL
        value: info
